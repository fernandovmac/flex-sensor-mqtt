### Squeeze me! 

This prototype streams the force on a flex sensor and a FSR over MQTT protocol

##### Hardware Description

Using a board Feather M0 and two sensors: a FSR and a Flex sensor. The wiring is still to be inclded in this repo.

##### Software Description

By activating the WiFi pins, the connection to the network is manages through the WiFi101 library. The two analog pins read the restistance of the sensors. The PubSub library handles the MQTT communication with the system.

